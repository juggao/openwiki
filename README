openwiki:  open the wikipedia lemma in the webbrowser via the commandline

INSTALL:
--------
sudo apt-get install python
pip install wptools
pip install termcolor
cp openwiki to ~/bin or somewhere in your $PATH
chmod u+x openwiki

